describe('Testing suite for "addBlock" function', () => {
    it('Should throw an exception when no arguments are provided', () => {
        // TODO: Fix import issues ES5/ES6 with jasmine
    });

    it('Should accept only strings as parameters, and throw exception otherwise', () => {
        // TODO: Fix import issues ES5/ES6 with jasmine
    });
})